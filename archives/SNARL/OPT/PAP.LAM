(define f (rec (lambda (fact n) (if (= n 0) 1 (* n (fact (1- n)))))))

(define f1 ( (lambda (x) (lambda (y) x (y y)) (lambda (y) x (y y)))
				 (lambda (fact n) (if (= n 0) 1 (* n (fact (1- n)))))))
